1.var is global scope declaration keyowrd thats menas we can use a variable out side a block scope function body...
let and const are block scope..that means that i cant use any variable outer side of the scope..if variable declear with let it and not initial a value it will give a undefinde value..but in const if i not declear a initial value it will trough a typeError..
we can use const only for values that must not change and not iterabiles...
but we can use let for iterabiles values...

2.the spread operator (...) is used to unpack or spread out the elements of an array or any iterabel obejct. In a context where individual values are expeced.

3.map() method basically return a new array. it dose not modifiy the invocked array
filter() method returns and array containing subset of the elements of the array on which it is invoked..
forEach() method iterates through an array,invoking a funcation specifiying for each element
4.arrow function is es6 function defining compact syntax and it dose not use function keyword insted of it use arrow like "=>" syntaxt..
5.In es6 or later string Literal can be delimitd with bakticks (``) and its more than just antother strig literal syntax
